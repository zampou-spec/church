generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:../../../prisma/database.db"
}

model Needed {
  id String @id @default(cuid())
  name String
  image String
  goal Decimal
  reached Decimal
}
